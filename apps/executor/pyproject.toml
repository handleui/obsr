[project]
name = "detent-executor"
version = "0.1.0"
description = "External executor for Detent autofix and healing"
requires-python = ">=3.11"
dependencies = [
    "modal>=0.64.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
