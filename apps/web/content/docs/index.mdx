---
title: Introduction
description: AI fixes your CI before you have to
---

Your CI breaks. You dig through logs. You fix a typo. You push again. You wait.

**Detent stops the cycle.** When your build fails, Detent reads the errors, writes a fix, and opens a PR. You review, merge, done.

## How It Works

1. **CI fails** — Your GitHub Action runs. ESLint, TypeScript, tests — something breaks.

2. **Detent parses the errors** — Our action extracts structured error data from your tools — not raw logs.

3. **AI writes the fix** — Claude reads your code, understands the error, and writes a patch.

4. **You approve** — Review the fix in a PR comment or the Detent dashboard. One click to apply.

## Get Started

- **[Quickstart](/docs/quickstart)** — 5 minutes to your first resolve
- **[Pricing](/docs/pricing)** — Pay per fix, not per seat

## What Can Detent Fix?

- **Linting errors** — ESLint, Biome, golangci-lint
- **Type errors** — TypeScript, Flow
- **Test failures** — Vitest, Jest, Go tests
- **Build errors** — Cargo, webpack, Next.js

Not magic. Detent fixes the errors that waste your time — the typos, the missing imports, the type mismatches. The stuff that's obvious once you see it.

## Questions?

- **[How Resolving Works](/docs/cli/how-resolving-works)** — The technical details
- **[Security & Trust](/docs/cli/trust)** — What Detent can access
