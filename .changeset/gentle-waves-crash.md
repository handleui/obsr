---
"@detent/api": minor
---

Add provider-based organization routes for GitHub/GitLab lookups.
New endpoints: `/orgs/:provider/:slug`, `/orgs/:provider/:slug/membership`,
`/orgs/:provider/:slug/projects`, and `/orgs/:provider/:slug/projects/:handle`.
