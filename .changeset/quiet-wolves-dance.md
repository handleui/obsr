---
"@detent/api": patch
---

Use stored GitHub identity from membership records as fallback when WorkOS doesn't have GitHub linked.
Require both providerUserId and providerUsername to avoid empty username in API calls.
