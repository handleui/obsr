{
  "name": "@detent/cli",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --manifest-path Cargo.toml",
    "check-types": "cargo check --manifest-path Cargo.toml",
    "test": "cargo test --manifest-path Cargo.toml",
    "dev": "cargo run --manifest-path Cargo.toml --"
  }
}
