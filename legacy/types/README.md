# @obsr/legacy-types

Legacy-only CI and resolver contracts for the old Observer apps.

This package exists to keep deprecated error and queue abstractions out of the active shared package roots.

Active ObsR code should use `@obsr/issues` and `@obsr/ai` instead.
