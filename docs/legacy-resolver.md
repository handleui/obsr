# Legacy Resolver

`apps/resolver` is a legacy sibling module.

Scope:
- optional AI patch generation path
- not required for Observer diagnostics adoption
- maintained separately from core Observer roadmap
