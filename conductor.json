{
  "scripts": {
    "setup": "bun install && ln -sf \"$CONDUCTOR_ROOT_PATH/apps/cli/.env\" apps/cli/.env && ln -sf \"$CONDUCTOR_ROOT_PATH/apps/navigator/.env\" apps/navigator/.env && ln -sf \"$CONDUCTOR_ROOT_PATH/packages/healing/.env\" packages/healing/.env && ln -sf \"$CONDUCTOR_ROOT_PATH/apps/api/.dev.vars\" apps/api/.dev.vars",
    "run": "bun run dev:core",
    "archive": ""
  }
}
