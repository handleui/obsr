# CLI required environment variables

# CLI WorkOS auth surface (deferred migration)
# Set this temporarily for local CLI device/browser auth testing.
# This path remains legacy until CLI migration is implemented.
WORKOS_CLIENT_ID=client_xxx
